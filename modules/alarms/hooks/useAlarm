import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { alarmService } from "../services/alarms.services";

export const useCreateAlarms = () => {
  const queryClient = useQueryClient();

  // actualizar el estado de la alarma. debe tener el Id del registro para acrualizar.
  // y agregarle un campo para mostrar el mensaje y el nuevo estado.
  return useMutation({
    mutationFn: alarmService.createAlarm,
    onSuccess: (res) => {
      if (res) {
      }

      // queryClient.invalidateQueries({ queryKey: ["users"] });
    },
  });
};
